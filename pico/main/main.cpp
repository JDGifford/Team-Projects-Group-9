#include <stdio.h>
#include "pico/stdlib.h"
#include <iostream>
#include <fstream>

int main()
{
    stdio_init_all();
    // gpio_init(19);
    // gpio_set_dir(19, GPIO_OUT);

    // while (true)
    // {
    //     // sleep_ms(1000);
    // }
    while (true)
    {
        // printf("Hello, world!\n")    gpio_pull_up(19);
    }
}
